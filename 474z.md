# keripy 2.0.0-dev6 cannot ingest the did:webs spec's own keri.cesr: the spec attaches an ACDC's proof as a CESR TransIdxSigGroup (-FAB, routed to exts['tsgs']), but parsing.py msgProcess's ACDC dispatch reads only exts['ssts'] (the -IAB source-seal triple this line's signing.serialize emits). processACDC is called with prefixer=None, saveCredential raises AttributeError, msgProcess re-raises it as 'No verifier to process so dropped ACDC', and the Parser swallows it. Sensor: tests/test_spec_stream.py asserts the exact e.proof.stream.frame.f rejection; the last test proves the credential saves when the -FAB triple is handed to processCredential directly. Consequences: didwebs cannot ingest 1.2.x-vintage published streams (the deployed did:webs ecosystem's direction of interop; our own crossimpl oracle only tests the other way). Fix upstream or handle tsgs at ingest; when it lands, rewrite the sensor as the acceptance golden it was meant to be.
kind: todo
created: 2026-08-15T02:56Z

- 2026-08-15T22:40Z Branch survey 2026-08-15 (probe clone of WebOfTrust/keripy): the ssts-only ACDC dispatch is IDENTICAL on v1.1.32, v1.2.14, v1.3.6, v2.0 and main (v2.0 and main have byte-identical parsing.py, both 2.0.0-dev6; no v2.1 branch exists). -FAB routes to tsgs on every line and no line's ACDC arm reads it. Measured live: the 1.2.13 GLEIF resolver ingests the spec stream (kever_sn 2) but silently drops the ACDC and derives alsoKnownAs:[] — a wrong document from the spec's own worked example. So this is not a 2.0-dev regression: NO keripy has ever verified the spec's -FAB ACDC proof form through the parser. Reframes the risk: deployed tooling that issues via keripy's signing.serialize emits -IAB and is fine; the -FAB form may exist only in the spec's example. Upstream material twofold: WebOfTrust keripy (parser gap) and the spec (canonical example unverifiable by any implementation).
