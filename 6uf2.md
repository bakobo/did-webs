# recovered_kel's branch rebuild is not pinned to the trunk it must hang off: _branch_head asserts the exploit and recovery frames share a prior event (tests/builders.py), but not that that prior IS the base stream's last KEL event. The fixture rests on the toolkit's determinism claim -- same salt, pinned registry nonce, fixed designation date rebuild the identical KEL -- and if that ever stopped holding, both branches would agree with each other and hang off nothing in the stream, so the failure would surface downstream as an out-of-order accounting rejection in test_ingest rather than as 'the fixture is not what it says'. Raised by the ds seat reviewing PR #6 (verdict SOUND, this was its one tightening); below the fix floor for that round, so filed rather than fixed. One assertion comparing the branch p against the trunk head closes it.
kind: debt
created: 2026-09-22T21:08Z

