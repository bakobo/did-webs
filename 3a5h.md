# qbqfst's version-string oracle has a blind spot: it asserts generated streams carry only KERI10JSON/ACDC10JSON version strings, which are EVENT BODY markers, and assemble._v1 checks serder.pvrsn -- the protocol version. Neither sees the CESR GENUS of the attachment counters, which is an independent axis. The ~3vol pin-bump attempt proved this is not theoretical: on bakobo/stack the ACDC attachment flips from a v1 '-I' SealSourceTriples to a v2 '-S' SealSourceCouples while every event body stays KERI10JSON/ACDC10JSON, so the oracle passes and only ingest.walk catches it. Wanted: extend the toolkit-wide qbqfst oracle to walk each emitted stream's attachment counters and assert every one decodes under CtrDex_1_0. That turns a v2-genus leak into a red test at the emit site instead of a confusing e.input.format.stream.f at ingest.
kind: todo
created: 2026-08-28T04:16Z
closed: 2026-09-15T06:09Z

- 2026-09-15T06:09Z DONE 2026-09-15 (1dc4bf3 intent, db90b62 code). Implemented as keri_api.v1_genus_violation: drive keripy's own Parser pinned to genus v1 and report where reading stopped, rather than hand-decoding counters. Same claim as 'every counter decodes under CtrDex_1_0' because Parser decodes each through Counter(version=Vrsn_1_0), which raises UnexpectedCodeError outside the v1 table (verified directly: -SAB decodes under v2, raises 'Unsupported code =-S' under v1) -- and it avoids reimplementing a CESR walk in test support, where a subtly wrong walk would be an oracle that lies. Asserted at all six ACCEPTED_VERSION_STRINGS sites plus tests/test_genus.py. The decisive test splices -IAB -> -SAB into a good base stream: version_strings still returns exactly {KERI10JSON, ACDC10JSON} (the blind spot, now demonstrated as an executable assertion) while the genus oracle names the ACDC frame at byte 2051. Exclusions are data on the fixture (builders facts gain deliberate_genus_violation), matching the deliberate_version_strings idiom. NOTE for ~3vol: this is now the red test at the emit site the bump needed -- re-attempting the bump should show it firing there instead of surfacing as e.input.format.stream.f at ingest.
