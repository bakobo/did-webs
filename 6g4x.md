# keri.cesr is not uniformly append-only, so the spec's prefix/subset divergence rule can fire on an honest republication: BADA reply state (/loc/scheme, /end/role/add) replaces rather than appends, and emit_stream replays only the reply records the current submission carried (assemble.py:279-281), so a controller who changes a witness URL publishes a stream the previously published one is neither a prefix nor a subset of -- and spec/body.md:190-192 says both streams and both DIDs are then invalid, permanently. Red-team A3. Two limbs: decide the publish-side behaviour (carry superseded reply records, or accept the gap and say so in this.i), and scope the comparison to the KEL/TEL portion in the phase-2 resolver
kind: todo
created: 2026-09-21T22:24Z

- 2026-09-22T00:09Z PUBLISH-SIDE LIMB DECIDED 2026-09-22: recorded as this.i tension q5qmjv3t rather than closed. We keep emit_stream replaying only the reply records the submission carried. Rejected carrying superseded reply records so every publication is a superset of the last -- it needs publication history the per-submission scratch database (embuup) deliberately does not keep, and it would republish endpoint advertisements the controller has withdrawn, which is what BADA's replace semantics exist to retire. Rejected detecting it at publish time: the publish side never sees the previously published artifact. The upstream ask is in scope.md soft spot 17 and in the ~4gab draft (scope the rule to KEL/TEL, define the relation once). WHAT REMAINS: the phase-2 resolver limb -- scope its own prefix comparison to the KEL and TEL portions.
