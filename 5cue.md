# Graduate e.input.range.stream.f into a shared Bakobo code registry: didwebs and did-webvh both declare the identical literal for the identical refusal (a submitted KERI CESR stream past its read bound). dev/standards/error-codes.md says genuinely common codes are 'defined once in the shared core registry and imported', but bakobo/errors ships the ErrorCode machinery with no shared catalog module to import from. Needs a home in bakobo/errors (or a decision that per-repo declaration of a shared literal is the estate's answer), plus a check that the catalog pipeline does not treat the duplicate declaration as a collision. Same shape as the open question in ~6ks5 about promoting heti's e.state.pending.witness.r.
kind: debt
created: 2026-09-15T06:00Z

