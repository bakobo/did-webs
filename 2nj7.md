# emit_stream derives registries from accepted credentials' regids: a registry the claimed AID incepted but never issued from is owned by ingest yet unemitted — hosting 'minus a frame'. Worker G could not construct the stream to demonstrate (second registry TEL clone failed); code-reading claim only. Fix would be in assemble.py; needs a two-registry fixture first
kind: todo
created: 2026-08-15T03:12Z
closed: 2026-09-15T06:27Z

- 2026-09-15T06:27Z DONE 2026-09-15 (1326524). CONFIRMED, not merely code-read: with two vcp frames in the submission, ingest accepts the stream and emit_stream produced one vcp. Worker G's blocker did not reproduce -- keri_api.tel_bytes clones the second registry's TEL fine (331 bytes); the new keri_api.incept_registry stops halfway through issue_aliases (makeRegistry + anchor + drain, no credential) and that is all it took.

Fix: registries are the union of accepted credentials' regids and accepted vcp frames' principals, issuing ones first so the reference's interleaved order is undisturbed for the single-credential case. New knob two_registries, a VALID fixture.

Interop checked rather than reasoned about, per gvimca: two_registries is now in ORACLE_FIXTURES and the GLEIF 1.2.13 resolver ingests the new shape and agrees on the derived document.
